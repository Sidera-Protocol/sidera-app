# Public runtime configuration (safe to commit).
# Leave the contract ID empty to run the app in demo mode with mock registry data.

# Deployed Sidera registry contract ID (C... string). Empty = demo mode.
NEXT_PUBLIC_SIDERIA_CONTRACT_ID=

# Network the app targets: testnet | futurenet
NEXT_PUBLIC_SIDERIA_NETWORK=testnet
